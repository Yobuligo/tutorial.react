// The simplest way to create a component is to use a function that returns JSX (JavaScript XML)
// It later can be embedded in another component

export function FirstComponent() {
  return (
    <div>
      <h1>Hello World</h1>
    </div>
  );
}
